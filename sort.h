#ifndef SORT_H
#define SORT_H

#include "type.h"

/* Ordena um vetor de inteiros em ordem decrescente.
* inputs: um vetor de inteiros, a posição inicial e final do vetor.
* output: nenhum.
* pre-condicao: nenhum. 
* pos-condicao: o vetor foi ordenado em ordem decrescente.
*/
void sort(Type *a, int lo, int hi);

#endif
